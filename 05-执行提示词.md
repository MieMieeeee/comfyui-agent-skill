你现在要在仓库 `MieMieeeee/run_comfyui_workflow` 中实现一个最小 MVP。

请认真阅读本项目提供的说明文件，并严格遵守。

## 核心目标

实现一个最小可复用的文本生图能力，固定使用内置 workflow：

- `workflows/z_image_turbo.json`

参考现有示例脚本：

- `run/get_zimage.py`

## 当前阶段定位

这是一个**小型 MVP**，不是通用平台。

你必须优先满足以下目标：

- 最小可运行实现
- 最大程度复用现有仓库逻辑
- 改动尽量小
- 结构清晰但不过度设计
- 为未来扩展留下空间，但不要因此引入过多复杂度

## 硬性约束

请不要做以下事情：

- 不要实现 MCP
- 不要实现通用 workflow registry
- 不要实现 workflow 自动发现
- 不要构建大型架构框架
- 不要重构整个仓库
- 不要为了单个固定 workflow 引入不必要复杂度

## 运行行为要求

最终实现必须满足：

1. 接收一个外部参数：
   - `prompt`

2. 固定使用 workflow：
   - `workflows/z_image_turbo.json`

3. 修改正向提示词节点文本

4. 其他 workflow 参数当前先固定

5. 使用现有 wrapper 执行 workflow

6. 将输出图片保存到本地

7. 返回结构化结果对象

## 重要技术偏好

请优先复用 `run/get_zimage.py` 中已经验证可行的执行方式。

重点复用：

- `ComfyApiWrapper`
- `ComfyWorkflowWrapper`
- `queue_and_wait_images(...)`

除非绝对必要，不要替换这些现有抽象。

## 你需要按顺序完成的事情

请按以下顺序执行：

1. 分析仓库结构与当前执行路径
2. 提出最小实现方案
3. 实现代码改动
4. 明确列出修改了哪些文件
5. 说明如何运行
6. 简要说明未来如何扩展到第二个 workflow

## 希望的输出风格

请务必以**可实施、可编码**为导向。

不要只停留在高层建议上。  
请直接提供具体代码改动。

如果你认为更简单的方案优于更分层的方案，请优先选择简单方案，并简短说明理由。